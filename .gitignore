_build/
_opam/
*.byte
*.native
.env

# Build artifacts
*.cmi
*.cmx
*.o

# Erlang leftovers from cross-repo experiments (not camlcoin artifacts)
erl_crash.dump
*.dump
feed-*.log
rebar.lock

# Claude config
.claude/

# Project-meta audit artifacts live in the meta-repo (audit-archive/).
audit/
CORE-PARITY-AUDIT/
